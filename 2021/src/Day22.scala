import scala.io.Source
import scala.language.implicitConversions
import scala.util.Using

object Day22 {

  private val positionRegex = """(on|off) x=(-?\d+)..(-?\d+),y=(-?\d+)..(-?\d+),z=(-?\d+)..(-?\d+)""".r
  def main(args: Array[String]): Unit = {
    Using(Source.fromFile("inputs/2021/input_day22.txt")) {
      source =>

    }
  }

}
